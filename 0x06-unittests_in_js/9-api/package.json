const { expect } = require('chai');

describe('Testing numbers', () => {
  it('1 is equal to 1', () => {
    expect(1).to.equal(1);
  });

  it('2 is greater than 1', () => {
    expect(2).to.be.greaterThan(1);
  });
});
